{
  "private": true,
  "scripts": {
    "install-client": "cd riotly-chat-client && yarn install",
    "install-server": "cd riotly-chat-server && yarn install",
    "install-all": "yarn install && yarn install-client && yarn install-server",
    "client": "cd riotly-chat-client && yarn run",
    "server": "cd riotly-chat-server && yarn run"
  },
  "devDependencies": {},
  "dependencies": {}
}

